#include <stdio.h>
#include <stdlib.h>
/**
 * main - the last digit of the number n
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int b;

	for (b = 48; b <= 57; b++)
	{
		putchar(b);
	}
	putchar('\n');
	return (0);
}
