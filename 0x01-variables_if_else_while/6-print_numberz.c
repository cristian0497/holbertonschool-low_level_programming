#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - the last digit of the number n
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char count;
for (count = '0'; count <= '9'; count++)
putchar (count);
putchar('\n');
return (0);
}
