#include <stdio.h>

/**
 * main - the last digit of the number n
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int character;
	for (character = 48; character <= 57; character++)
	{
		putchar(character);
	}
putchar('\n');
return (0);
}
