#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - the last digit of the number n
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int character = 48;
while (character <= 57)
{
putchar(character);
character++;
}
putchar('\n');
return (0);
}
